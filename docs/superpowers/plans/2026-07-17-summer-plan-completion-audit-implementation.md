# 暑期作业计划收口实施计划

日期：2026-07-17  
依据：`2026-07-17-summer-plan-completion-audit-design.md`

## 批次 A：来源契约与数据纠偏

1. 为五份执行 CSV 增加显式任务阶段列，补齐六科任务阶段。
2. 修正语文七个正式批次与《红楼梦》范围；把征文改为待确认建议任务。
3. 将数学作业 2 按 11/22/15 题拆为三个真实子卷块。
4. 统一所有“本系统上传”歧义文案，保留真实学校渠道。
5. 升级导入器、类型和 JSON 版本，重新生成计划。
6. 增加正式要求断言，而非只验证 CSV 与 JSON 自洽。

验收：源验证、计划单元测试、数据摘要测试全部通过；高严重度 `math-under-split` 从待解决问题中移除。

## 批次 B：动态日期、容量与任务体验

1. 新增上海时区计划参考日期函数，支持测试注入。
2. 替换首页、风险、孩子今日任务、创建任务默认日期和周报中的静态日期。
3. 将容量风险拆成独立作业容量、课内任务和当日总负荷证据。
4. 明确展示“本系统只标记，不上传作业”。
5. 检查移动端 390px 的日期、冲突、任务和批改页面。

验收：边界日期、时区、周报周起点、容量风险和移动端渲染测试通过。

## 批次 C：同步安全与数据库升级

1. 新增迁移支持目录版本与实例版本提示，不修改已应用迁移。
2. 升级模板同步：校验摘要、清理未实例化旧模板、拒绝覆盖已实例任务。
3. 在本地数据库执行完整迁移、RLS 和工作流集成测试。
4. 生成可回滚发布包和数据库备份。

验收：迁移从空库和现有库均通过；模板数量、摘要和孤儿检查一致；旧家庭实例保护测试通过。

## 批次 D：线上发布与真实角色回归

1. 用本地 Git bundle 发布到 `/srv/summerwork/app`，构建新镜像。
2. 先备份数据库，再迁移、同步模板，最后原子替换应用容器。
3. 执行家长、两名不同科家教、孩子四账号回归。
4. 验证邀请一次性、分科权限、计划调整通知、学生轻交互、双确认和双轨点亮。
5. 验证 HTTPS、Realtime、备份、恢复抽查、容器资源和其他项目健康状态。

验收：线上要求矩阵无高严重度差异；所有容器无异常重启；服务器其他项目状态不变。

## 批次 E：交付收口

1. 更新 README、验收追踪表、家长/家教/孩子简明手册和运维说明。
2. 输出正式来源到系统行为的逐条证据表。
3. 记录仍需学校或家长确认的外部事项，不把它们伪装成已完成。
4. 首位家长邮箱由用户提供后，仅创建父级入口；密码由用户自行设置，系统不收集密码。

验收：文档与线上版本、数据摘要、迁移号和测试证据一致。

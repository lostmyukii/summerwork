# 独立预习双轨生产发布记录

## 结果

2026-07-17 已将独立预习线发布到腾讯云 Summerwork 私有栈。线上应用使用提交 `95cf4c3`，正式家庭保留原有 203 条学校作业任务，并新增 23 节家教带学预习；两条线的数据、状态和权限完全独立。

## 恢复检查点

- 服务器检查点：`/srv/summerwork/checkpoints/20260717T082255Z`
- 迁移前数据库备份：`/srv/summerwork/backups/summerwork-postgres-20260717T082255Z.dump`
- 备份 SHA-256：校验通过
- 旧应用回滚镜像：`summerwork_app:rollback-prestudy-20260717T082256Z`
- 新应用镜像：`sha256:540a5f508c3ff5f80580f7f87f2d89e065cfccd335d1de3b80e692bc201dbee3`
- GitHub `main`：已推送到 `95cf4c3`

## 数据库与数据同步

- 迁移从 20 份增加到 21 份，新增 `0021_independent_prestudy_track.sql`。
- 新增预习课、课程槽、预设知识点、执行记录和未掌握项；只读视图、分科 RLS、受控 RPC、站内通知和 Realtime 已启用。
- 先同步 1 节代表课，再同步全部 23 节，随后重复全量同步验证幂等。
- 最终数量：预习 23 节、有效课程槽 62 个、有效预设知识点 90 个。
- 科目数量：数学 5、物理 5、化学 4、生物 4、俄语 3、语文 2。
- 8 月 12 日预习为 0；8 月 14 日为数学 M03 与物理 P03 共 2 节。
- 学校作业任务始终保持 203 条；预习执行记录和预习审计均为 0，没有提前完成正式任务。

## 家教内容编辑权限

- 分科家教可修改本科预习标题、四阶段带学内容、验收标准和 1—12 个预设知识点。
- 内容修改必须填写原因，并以版本号、操作者和时间写入审计，同时向家长发送站内通知。
- 旧知识点只停用、不物理删除，既有验收证据不会丢失；来源再次同步不会覆盖家教版本。
- 家长与孩子只读；跨科家教修改被数据库命令拒绝。

## 验收证据

- 本地：源数据追溯、TypeScript、ESLint、71 项单元测试、本地 PostgreSQL 集成、生产构建、4 项渲染测试、4 项部署结构测试和生产依赖审计通过。
- 浏览器：桌面和 390px 手机无横向溢出；日历上方预习线、下方作业线；家长只读，家教可完成内容编辑、带学和验收。
- 线上：六科家教分别只读取 5/5/4/4/3/2 节；孩子读取 23 节；家长会话读取 23 节预习和 203 条学校作业。
- 写入权限：数学家教内容修改成功路径、物理家教跨科拒绝均在事务内验证并回滚，正式数据没有留下测试事件。
- 公网：登录页返回 200；应用、Auth、REST、Realtime 和 Kong 健康检查通过；Nginx 语法通过。

## 服务器隔离

- 只重建 `summerwork_app`，使用 `--no-deps`，未停止或重建数据库、Auth、REST、Realtime、Kong。
- Summerwork 仍只监听 `127.0.0.1:3180` 和 `127.0.0.1:8180`。
- staging 与 MQTT 等既有容器保持运行，重启数 0、OOM 为 false。
- 未执行全局 Docker 清理、服务器重启、端口或 Nginx 站点变更。

## 账号说明

- 家长自行注册账号的密码未修改。
- 六科家教与孩子共 7 个预置账号已统一为符合生产策略的 12 位初始密码，并逐个完成真实密码登录。
- 初始密码不写入 Git；交接后应由使用者尽快改为个人密码。

## 回滚边界

应用问题只回退到 `summerwork_app:rollback-prestudy-20260717T082256Z`，并继续使用 `--no-deps` 创建 `summerwork_app`。数据库迁移为只增不删；若需数据库恢复，使用本次项目专属备份和既有恢复流程。不得执行 `down -v`、全局清理、服务器重启或改动其他项目资源。

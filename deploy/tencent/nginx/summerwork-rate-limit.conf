# Install in /etc/nginx/conf.d/. This adds project-scoped zones only.
map $http_upgrade $summerwork_connection_upgrade {
    default upgrade;
    ''      close;
}

limit_req_zone $binary_remote_addr zone=summerwork_auth:10m rate=10r/m;
